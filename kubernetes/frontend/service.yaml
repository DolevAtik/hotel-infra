apiVersion: v1
kind: Service
metadata:
  name: frontend
  namespace: hotel
  labels:
    app: hotel
    component: frontend
spec:
  type: LoadBalancer
  selector:
    app: hotel
    component: frontend
  ports:
    - name: http
      port: 80
      targetPort: 80
